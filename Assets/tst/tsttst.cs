using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tsttst : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

        //var sl = new SortedList<int, string>();
        //sl.Add(3, "33333");
        //sl.Add(2, "22222");

        //Debug.Log(sl.get);

    }

    // Update is called once per frame
    void Update()
    {

    }
}
